platform :ios, '12.0'

# Remove `use_frameworks!` (or use the newer syntax)
# use_frameworks!
use_frameworks! :linkage => :static

target 'Runner' do
  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
end
